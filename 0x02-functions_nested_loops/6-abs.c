#include "main.h"
/**
  *_abs - Absolute value in this world
  *Description: 'You can only estimate what a thing is worth'
  *@i: integer number inputted
  *Return: absolute number
  */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
