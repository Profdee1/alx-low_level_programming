#include "main.h"
/**
  *print_alphabet - alphabet game
  *Description: 'prints the alphabet, in lowercase'
  *Return: always 0
  */
void print_alphabet(void)
{
	int ch;

	for (ch = 97; ch <= 122; ch++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
